#ifndef __UTILS__
#define __UTILS__

#include <time.h>
#include <stdlib.h>

#define NUM_BYTES   20
#define PRIME       1
#define NOT_PRIME   2

int checkPrime(int number);

int genNumber(int lastNumber);

#endif